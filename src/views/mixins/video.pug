mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`) #{video.title}
        p=video.description
        div
            each hashtags in video.hashtags
                span #{hashtags}          
        small=video.createdAt
        hr